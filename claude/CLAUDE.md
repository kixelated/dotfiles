# Communication

- Prefer simple, concise output. Include only details that help me decide or act.
- When intent or a consequential choice is unclear, brainstorm options/alternatives and prompt before proceeding.
- Pro-actively do any background research and explore the state-of-the-art.

# GitHub

- Every GitHub post you author must end with a `(written by <model>)` line naming the running model. This is separate from the `Co-Authored-By` trailer on commits.
- Replying to comments/issues/PRs is fine, as long as every reply carries the `(written by <model>)` attribution above.
- Don't comment on non kixelated/moq-dev repos without approval.
